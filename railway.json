{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install && npx prisma generate --schema=apps/server/prisma/schema.prisma && npm run build -w @tt/server"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy --schema=apps/server/prisma/schema.prisma && node apps/server/dist/src/index.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120
  }
}
